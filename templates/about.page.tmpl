{{template "base" .}}

{{define "content"}}
<div class="container">
    <div class="row">
        <div class="col">
            <h1>This is the About page</h1>
            <p>Start stuff here!</p>

            <h2>{{ index .StringMap "hello"}}</h2>

            <p>
                {{ if ne (index .StringMap "remote_ip") ""}}
                Your remote IP address is {{index .StringMap "remote_ip"}}
                {{else}}
                I don't know your IP address. Visit <a href="/">Homepage</a> and the come back!
                {{end}}
            </p>
        </div>
    </div>
</div>

{{end}}